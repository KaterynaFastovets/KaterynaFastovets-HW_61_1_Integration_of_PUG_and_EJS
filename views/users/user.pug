doctype html
html
  head
    title= "Користувач"
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css")
  body
    .container(style="background: linear-gradient(to right, #ada996, #f2f2f2, #dbdbdb, #eaeaea); min-height: 100vh; display: flex; flex-direction: column; align-items: center; justify-content: center;")
        h1.text-center.mb-4 Деталі користувача
        p.text-center.mb-4
          strong Ім'я: #{user.name}
        p.text-center.mb-4 
          strong Email: #{user.email}
        a.btn.btn-secondary(href='/users') Назад до списку користувачів
